services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    restart: unless-stopped
    ports:
      - "4173:4173"
    environment:
      VITE_API_URL: ${VITE_API_URL}
    command: sh -c "pnpm build && pnpm preview --host 0.0.0.0 --port 4173"
